{
  test {
    spark {
      home = ${SPARK_HOME}
      app {
        jar  = ${SPARK_HOME}"/examples/jars/spark-examples_2.11-2.2.1.jar",
        main = "org.apache.spark.examples.SparkPi",
        args = [ "10" ]
      }
    }
  }
}